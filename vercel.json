{
    "framework": null,
    "github": { "silent": true },
    "routes": [{ "src": "/.*", "dest": "/api/vercel.ts" }],
    "builds": [
    {
        "src": "api/**/*.ts",
        "use": "@vercel/node"
    }
]
}
